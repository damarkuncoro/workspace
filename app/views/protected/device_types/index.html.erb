<%# DeviceTypes Index: konsisten dengan halaman lain menggunakan shared page_header dan KT helpers %>
<%= render 'protected/shared/partials/page_header',
  title: 'Device Types',
  subtitle: 'Kelola kategori dan spesifikasi perangkat',
  actions_html: (link_to new_protected_device_type_path, class: "kt-btn kt-btn-primary" do
    raw('<i class="ki-filled ki-plus"></i> Tipe Baru')
  end) %>

<%# Metrics ringkas menggunakan KT cards %>
<%= render 'protected/device_types/partials/metrics' %>

<%# Toolbar filter/pencarian %>
<%= render 'protected/device_types/partials/filters_toolbar' %>

<%# Tabel daftar tipe perangkat %>
<%= render 'protected/device_types/partials/table' %>
