<%# View: New DeviceType
  Tujuan: Konsistensi layout dengan page_header, kt_card, dan page_actions.
  Prinsip: DRY — gunakan partial wrapper new_form.
%>

<%= render 'protected/shared/partials/page_header',
  title: 'Buat Tipe Perangkat Baru',
  subtitle: 'Tambah kategori perangkat baru untuk klasifikasi' %>

<%= render 'protected/device_types/partials/new_form' %>

<%= render 'protected/shared/partials/page_actions',
  card: false,
  justify: 'justify-center',
  links: [
    link_to(protected_device_types_path, class: "kt-btn kt-btn-outline") do
      'Kembali ke Daftar Tipe'
    end
  ] %>
