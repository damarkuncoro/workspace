<%# View: Edit DeviceType
  Tujuan: Konsistensi layout dengan page_header, kt_card, dan page_actions.
  Prinsip: DRY — gunakan partial wrapper edit_form.
%>

<%= render 'protected/shared/partials/page_header',
  title: 'Ubah Tipe Perangkat',
  subtitle: "Perbarui informasi untuk #{@device_type.name}" %>

<%= render 'protected/device_types/partials/edit_form' %>

<%= render 'protected/shared/partials/page_actions',
  card: false,
  justify: 'justify-center',
  links: [
    link_to(protected_device_type_path(@device_type), class: "kt-btn kt-btn-outline") { 'Kembali ke Detail Tipe' },
    link_to(protected_device_types_path, class: "kt-btn kt-btn-secondary") { 'Kembali ke Daftar Tipe' }
  ] %>
