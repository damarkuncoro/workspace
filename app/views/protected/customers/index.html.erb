<%#
  View: Customers Index (Protected)
  Tujuan: Selaraskan dengan pola Accounts Index menggunakan KT helpers dan partial table.
  Prinsip: DRY, SRP, KISS — pecah table ke partial, header actions terpusat.
%>

<%= render 'protected/shared/partials/page_header',
  title: 'Customers',
  subtitle: 'Manage customer accounts and information',
  actions_html: (link_to new_protected_customer_path, class: 'kt-btn kt-btn-primary' do
    raw('<i class="ki-filled ki-plus"></i> New Customer')
  end) %>

<%= render 'protected/customers/partials/table' %>