<%#
  Partial: Accounts Page Header (Unified)
  Tujuan: Satu partial header untuk semua halaman Accounts (index, show, new, edit).
  Prinsip: DRY, SRP, KISS — menerima locals untuk title, subtitle, dan optional actions.
  Locals:
    - title: String judul utama
    - subtitle: String deskripsi/subjudul
    - actions_html: Optional HTML untuk tombol aksi di sisi kanan
  Pemakaian:
    <%= render 'protected/accounts/partials/page_header', title: 'Accounts', subtitle: 'Manage users', actions_html: (link_to ... ) %>


<% actions = local_assigns[:actions_html] %>

<%= kt_content_container do %>
  <div class="flex flex-wrap items-center lg:items-end justify-between gap-5 pb-7.5">
    <div class="flex flex-col justify-center gap-2">
      <h1 class="text-xl font-medium leading-none text-mono">
        <%= title %>
      </h1>
      <% if subtitle.present? %>
        <div class="flex items-center gap-2 text-sm font-normal text-secondary-foreground">
          <%= subtitle %>
        </div>
      <% end %>
    </div>
    <% if actions.present? %>
      <div class="flex items-center gap-2.5">
        <%= actions %>
      </div>
    <% end %>
  </div>
<% end %>