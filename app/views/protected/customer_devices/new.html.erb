<%# View: Protected::CustomerDevices#new
    Tujuan: Halaman pembuatan rental perangkat untuk Customer.
    Referensi: Mengikuti pola Accounts#new (page_header, partial new_form, page_actions).
    Prinsip: SRP, DRY, KISS, YAGNI, Rails-like.
    Catatan: Gunakan helper KT untuk kontainer dan actions. %>

<%= render 'protected/shared/partials/page_header',
  title: 'Sewa Perangkat',
  subtitle: "Buat rental untuk #{@customer.person_name}" %>

<%= render 'protected/customer_devices/partials/new_form' %>

<%= render 'protected/shared/partials/page_actions',
  card: false,
  justify: 'justify-center',
  links: [
    link_to(protected_customer_devices_path(@customer), class: "kt-btn kt-btn-outline") do
      raw('<i class="ki-filled ki-arrow-left"></i> Kembali ke Rentals')
    end
  ] %>
