<%#
  Halaman: Index Perangkat
  Tujuan: Konsisten dengan halaman lain memakai helper `page_header` dan partial tabel.
  Prinsip: SRP, KISS, DRY — index hanya header + render partial tabel.
%>

<%= render 'protected/shared/partials/page_header',
            title: 'Perangkat',
            subtitle: 'Kelola inventaris perangkat',
            actions_html: (
              link_to new_protected_device_path, class: 'kt-btn kt-btn-primary' do
                raw('<i class="ki-filled ki-plus"></i> Tambah Perangkat')
              end
            ) %>

<%= render 'protected/devices/partials/table',
            records: @devices,
            namespace: :protected,
            resource_singular: :device,
            resource_plural: :devices %>
