<%# Template: People#edit - gunakan page_header & partial form %>
<%= render 'protected/shared/partials/page_header',
  title: 'Ubah Orang',
  subtitle: 'Perbarui informasi orang' %>

<%= render 'protected/people/partials/edit_form' %>

<%= render 'protected/shared/partials/page_actions',
  card: false,
  justify: 'justify-center',
  links: [
    link_to(protected_person_path(@person), class: "kt-btn kt-btn-outline") do
      raw('<i class="ki-filled ki-eye"></i> Lihat Orang')
    end,
    link_to(protected_people_path, class: "kt-btn kt-btn-outline") do
      raw('<i class="ki-filled ki-arrow-left"></i> Kembali ke Daftar Orang')
    end
  ] %>
