<%# Template: People#new - gunakan page_header & partial form %>
<%= render 'protected/shared/partials/page_header',
  title: 'Tambah Orang',
  subtitle: 'Tambahkan data orang baru ke sistem' %>

<%= render 'protected/people/partials/new_form' %>

<%= render 'protected/shared/partials/page_actions',
  card: false,
  justify: 'justify-center',
  links: [
    link_to(protected_people_path, class: "kt-btn kt-btn-outline") do
      raw('<i class="ki-filled ki-arrow-left"></i> Kembali ke Daftar Orang')
    end
  ] %>
