<%#
  Halaman: Index People
  Tujuan: Konsistensi layout dengan helper `page_header` dan partial tabel KT.
  Prinsip: SRP/KISS/DRY — header + render partial tabel.
%>

<%= render 'protected/shared/partials/page_header',
            title: 'Orang',
            subtitle: 'Kelola data orang',
            actions_html: (
              link_to new_protected_person_path, class: 'kt-btn kt-btn-primary' do
                raw('<i class="ki-filled ki-plus"></i> Tambah Orang')
              end
            ) %>

<%= render 'protected/people/partials/table',
            records: @people,
            namespace: :protected,
            resource_singular: :person,
            resource_plural: :people %>