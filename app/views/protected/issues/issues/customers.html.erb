<%= render 'protected/shared/partials/page_header',
  title: 'Issues Customer',
  subtitle: 'Kelola tiket dukungan customer',
  actions_html: safe_join([
    link_to(protected_issues_path, class: 'kt-btn kt-btn-outline') do
      raw('<i class="ki-filled ki-category"></i> Semua Issues')
    end,
    link_to(protected_issues_employees_path, class: 'kt-btn kt-btn-outline') do
      raw('<i class="ki-filled ki-people"></i> Issues Karyawan')
    end
  ])
%>

<%= render partial: 'protected/issues/issues/partials/table',
  locals: {
    records: @issues,
    namespace: :protected,
    resource_singular: :issue,
    resource_plural: :issues
  } %>