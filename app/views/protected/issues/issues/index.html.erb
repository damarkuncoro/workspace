<%#
  View: Issues Index (Protected)
  Tujuan: Selaraskan dengan pola Accounts Index menggunakan KT helpers dan partial table.
  Prinsip: DRY, SRP, KISS — header menggunakan shared partial, table dipisah ke partial.
%>

<%= render 'protected/shared/partials/page_header',
  title: 'Support Issues',
  subtitle: 'Track and manage customer and employee support tickets',
  actions_html: safe_join([
    link_to(protected_issues_customers_path, class: 'kt-btn kt-btn-outline') do
      raw('<i class="ki-filled ki-user"></i> Customer Issues')
    end,
    link_to(protected_issues_employees_path, class: 'kt-btn kt-btn-outline') do
      raw('<i class="ki-filled ki-people"></i> Employee Issues')
    end
  ]) %>

<%= render 'protected/issues/issues/partials/table' %>