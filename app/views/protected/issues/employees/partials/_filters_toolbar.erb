<%# Toolbar: Filter Issues untuk Karyawan (Tailwind) %>
<%# Tujuan: Menyediakan filter status, priority, dan pencarian title/description via GET %>
<%= kt_card do %>
  <%= kt_card_content(padding: 'p-4') do %>
    <%= form_with url: protected_employee_issues_path(@employee), method: :get, local: true do %>
      <%= kt_content_grid(cols: 1, gap: 'gap-4', class_name: 'sm:grid-cols-2 lg:grid-cols-4') do %>
        <div>
          <label class="block text-sm font-medium text-gray-700">Status</label>
          <%= select_tag :status,
            options_for_select([["All", ""]] + @status_options.map { |s| [s.humanize, s] }, params[:status]),
            class: 'mt-1 block w-full rounded-md border-gray-300 bg-white py-2 pl-3 pr-10 text-sm shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500' %>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700">Priority</label>
          <%= select_tag :priority,
            options_for_select([["All", ""]] + @priority_options.map { |p| [p.humanize, p] }, params[:priority]),
            class: 'mt-1 block w-full rounded-md border-gray-300 bg-white py-2 pl-3 pr-10 text-sm shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500' %>
        </div>
        <div class="lg:col-span-2">
          <label class="block text-sm font-medium text-gray-700">Search</label>
          <%= text_field_tag :search, params[:search], placeholder: 'Search title or description…', class: 'mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm' %>
        </div>
      <% end %>
      <div class="mt-4 flex items-center gap-3">
        <%= button_tag class: 'inline-flex items-center rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700' do %>
          <i class="ki-filled ki-filter mr-2"></i> Filter
        <% end %>
        <%= link_to protected_employee_issues_path(@employee), class: 'inline-flex items-center rounded-md bg-white px-4 py-2 text-sm font-semibold text-gray-700 ring-1 ring-inset ring-gray-300 hover:bg-gray-50' do %>
          <i class="ki-duotone ki-arrow-rotate-left mr-2"></i> Reset
        <% end %>
      </div>
    <% end %>
  <% end %>
<% end %>