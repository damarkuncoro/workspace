<%#
  View: Employees Index (Protected)
  Tujuan: Selaraskan dengan pola Accounts Index menggunakan KT helpers dan partial table.
  Prinsip: DRY, SRP, KISS — table dipisah ke partial, header actions terpusat.
%>

<%= render 'protected/shared/partials/page_header',
  title: 'Employees',
  subtitle: 'Manage employee accounts and information',
  actions_html: (link_to new_protected_employee_path, class: 'kt-btn kt-btn-primary' do
    raw('<i class="ki-filled ki-plus"></i> New Employee')
  end) %>

<%= render 'protected/employees/partials/table' %>